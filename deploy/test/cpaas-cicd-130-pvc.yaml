apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-s3fs-130-pvc
  namespace: cpaas-cicd
spec:
  storageClassName: minio-s3fs-sc
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteOnce

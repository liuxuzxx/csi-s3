apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-s3-minio
  namespace: minio
spec:
  storageClassName: minio-csi-s3-xw
  resources:
    requests:
      storage: 20Gi
  accessModes:
    - ReadWriteOnce

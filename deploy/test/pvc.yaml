apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-s3-minio
  namespace: minio
spec:
  storageClassName: mounter-bucket-xw-sc
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteOnce

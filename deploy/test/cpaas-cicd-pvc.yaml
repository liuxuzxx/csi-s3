apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-cpaas-pvc
  namespace: cpaas-cicd
spec:
  storageClassName: minio-csi-s3-xw
  resources:
    requests:
      storage: 100Gi
  accessModes:
    - ReadWriteOnce

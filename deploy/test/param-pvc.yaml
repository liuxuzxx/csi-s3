apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: param-pvc
  namespace: minio
spec:
  storageClassName: minio-csi-s3-xw
  resources:
    requests:
      storage: 15Gi
  accessModes:
    - ReadWriteOnce
